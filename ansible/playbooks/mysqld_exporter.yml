---
# Owner: 김상희
# Scope: Data, Storage & Recovery / Observability
# Project: 石나가는 판단
# Purpose: Install and configure mysqld_exporter on MariaDB Guest VMs

- name: Install and configure mysqld_exporter on MariaDB Guest VMs
  hosts: mariadbs
  become: true
  gather_facts: true
  roles:
    - mysqld_exporter
