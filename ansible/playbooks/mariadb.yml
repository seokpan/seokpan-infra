---
# Owner: 김상희
# Scope: Data, Storage & Recovery
# Project: 石나가는 판단
# Purpose: Install MariaDB server package (version pinned)

- name: Install MariaDB server package
  hosts: mariadbs
  become: true
  gather_facts: true

  roles:
    - mariadb