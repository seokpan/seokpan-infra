---
# Owner: 김상희
# Scope: Data, Storage & Recovery / Observability
# Project: 石나가는 판단
# Purpose: Provision MaxScale REST API read-only user for maxscale_exporter (exporter install is deferred to phase 2)

- name: Provision MaxScale REST API read-only user (maxscale_exporter)
  hosts: maxscale
  become: true
  gather_facts: true
  roles:
    - maxscale_exporter
