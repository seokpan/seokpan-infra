---
# Owner: 김상희
# Scope: Data, Storage & Recovery
# Project: 石나가는 판단
# Purpose: nfs-utils package installation (version pinned, CentOS Stream baseos)

- name: Install pinned nfs-utils package
  ansible.builtin.dnf:
    name: "{{ nfs_server_packages }}"
    state: present
