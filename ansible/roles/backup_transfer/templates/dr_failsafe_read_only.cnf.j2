# DR 임시 안전장치 (이슈 #119) — 자동 생성 파일, 수동 편집 금지
# 이 서버가 정상 복제(replica)로 재편입되면 이 파일은 자동으로 제거됩니다.
# 목적: 양쪽 다 유실된 DR 상황에서 다른 한쪽이 새 Master로 확립된 뒤,
# 이 서버가 복제 설정 없이 그냥 기동되어 MaxScale에 의해 "고립된 Master"로
# 오판정되는 것을 방지(Split-brain 방지). 부팅 즉시 read_only 상태로 시작하도록 강제.
[mariadb]
read_only=1
