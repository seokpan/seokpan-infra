---
# Owner: 정태훈
# Scope: Kubernetes / Service Platform
# Project: 石나가는 판단
# Purpose: kubeadm_worker default variables

kubeadm_worker_join_source: "cp-01"
kubeadm_worker_join_token_ttl: "15m"

kubeadm_worker_kubelet_conf: "/etc/kubernetes/kubelet.conf"

kubeadm_worker_packages:
  - nfs-utils
