---
# Owner: 정태훈
# Scope: Kubernetes / Service Platform
# Project: 石나가는 판단
# Purpose: container_runtime handlers

- name: Restart containerd
  ansible.builtin.systemd:
    name: containerd
    state: restarted
